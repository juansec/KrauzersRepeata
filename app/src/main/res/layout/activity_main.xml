<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:scrollbars="vertical"
    android:fadeScrollbars="false"
    android:clipToPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <Button
            android:id="@+id/btnLoadRequest"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Cargar request .txt" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="REQUEST"
            android:textStyle="bold" />

        <EditText
            android:id="@+id/editRequest"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:gravity="top|start"
            android:hint="La request aparecerá aquí..."
            android:inputType="textMultiLine"
            android:fontFamily="monospace"
            android:scrollbars="vertical" />

        <Switch
            android:id="@+id/switchHttps"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="HTTPS"
            android:checked="true" />

        <Button
            android:id="@+id/btnSend"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Enviar request" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="RESPONSE"
            android:textStyle="bold" />

        <EditText
            android:id="@+id/editResponse"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:gravity="top|start"
            android:hint="La respuesta aparecerá aquí..."
            android:inputType="textMultiLine"
            android:fontFamily="monospace"
            android:scrollbars="vertical"
            android:focusable="false" />
        <Space
            android:id="@+id/imeSpacer"
            android:layout_width="match_parent"
            android:layout_height="0dp" />

    </LinearLayout>

</ScrollView>